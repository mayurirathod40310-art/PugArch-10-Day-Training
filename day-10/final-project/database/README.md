# Database

The current version of the Smart Hygiene Risk Prediction System does not require a persistent database.

Prediction history is maintained temporarily in the React frontend during the current browser session.

A relational database such as PostgreSQL or MySQL can be integrated in a future version to store facility records, prediction history, timestamps, and user information.
